# SMTP Configuration for Email Feature
# Choose settings based on your email provider:

# --- Siteground (SSL on port 465) ---
SMTP_HOST=mail.leedurbin.co.nz
SMTP_PORT=465
SMTP_USERNAME=hello@leedurbin.co.nz
SMTP_PASSWORD=your-email-password
SMTP_FROM_EMAIL=hello@leedurbin.co.nz

# --- Gmail (TLS on port 587) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM_EMAIL=your-email@gmail.com

# --- Generic SMTP (Auto-detects SSL/TLS based on port) ---
# SMTP_HOST=mail.yourprovider.com
# SMTP_PORT=587  # Use 587 for TLS or 465 for SSL
# SMTP_USERNAME=your-email@domain.com
# SMTP_PASSWORD=your-password
# SMTP_FROM_EMAIL=your-email@domain.com

# Optional: Test Email Integration
TEST_EMAIL_INTEGRATION=false
TEST_EMAIL_ADDRESS=test@example.com
